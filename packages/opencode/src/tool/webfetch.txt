Fetch content from a URL.

- URL is required (http:// or https://); format is optional (markdown default).
- timeout is optional (seconds, max 120).
- Responses larger than 5MB are rejected.
- Image responses are returned as attachments.
