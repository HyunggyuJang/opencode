Replace exact text in a file.

- Read the file first.
- oldString must match exactly, including whitespace; do not include line-number prefixes.
- oldString must be unique unless replaceAll is true.
- Use replaceAll for renames.
