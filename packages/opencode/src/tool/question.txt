Ask the user for clarification or choices.

- If custom is enabled, "Type your own answer" is added; avoid "Other" options.
- Answers return as labels; set multiple for multi-select.
- Put the recommended option first and label "(Recommended)".
