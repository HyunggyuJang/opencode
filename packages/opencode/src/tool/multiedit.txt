Apply multiple edits to one file in one operation.

- Read the file first; file_path must be absolute.
- Each edit uses oldString/newString and optional replaceAll.
- Edits apply in order and are atomic; any failure aborts.
- Same matching rules as Edit (exact match, unique unless replaceAll).
