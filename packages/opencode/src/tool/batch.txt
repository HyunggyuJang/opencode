Run multiple independent tool calls in parallel.

- Input object: {"tool_calls": [{"tool": "name", "parameters": {...}}]}
- 1-25 calls per batch
- Calls execute in parallel; failures do not stop other calls
- Avoid dependent steps or nested batches
